name: pipeline
version: 3.6.0
description: "Pipeline Plugin v3.6.0 — code-review-graph integration: agents @reviewer and @security use MCP-powered graph for blast radius, risk scoring, and test gap detection. 38-528× token savings on code review."
author: Hermes + Vladimir
provides_tools:
  - pipeline_classify
  - pipeline_convergence
  - pipeline_save
  - pipeline_load
  - pipeline_clear
  - pipeline_resume
  - pipeline_advance
  - agent_prompt
  - agent_model
  - pipeline_run_agent
  - pipeline_ensemble_run
  - pipeline_ensemble_judge
