name: pipeline
version: 3.3.0
description: "Pipeline Plugin v3.3.0 — SQLite-native kanban. Selective context. 20 bugfixes from ARCHITECTURE-FIXES.md. Best-of-N ensemble. Retrospective logging + auto-analysis. Convergence fixed (status filter). Hot-reload MODEL_MAP. Default prompt fallback."
author: Hermes + Vladimir
provides_tools:
  - pipeline_classify
  - pipeline_convergence
  - pipeline_save
  - pipeline_load
  - pipeline_clear
  - pipeline_resume
  - pipeline_advance
  - agent_prompt
  - agent_model
  - pipeline_run_agent
  - pipeline_ensemble_run
  - pipeline_ensemble_judge
