name: pipeline
version: 3.5.1
description: "Pipeline Plugin v3.5.1 — retro logging fixes: agent_done now writes duration_s/tokens_response/status, tokens_prompt counted from real prompt, pipeline_resume logs metadata, ADVANCE_SCHEMA extended with optional fields."
author: Hermes + Vladimir
provides_tools:
  - pipeline_classify
  - pipeline_convergence
  - pipeline_save
  - pipeline_load
  - pipeline_clear
  - pipeline_resume
  - pipeline_advance
  - agent_prompt
  - agent_model
  - pipeline_run_agent
  - pipeline_ensemble_run
  - pipeline_ensemble_judge
