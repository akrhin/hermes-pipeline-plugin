name: pipeline
version: 1.2.0
description: "Multi-agent pipeline orchestrator. Classifies requests, builds pipelines, manages state, delegates to specialized agents. v1.2.0: @integration agent + Kanban."
author: Hermes + Vladimir
provides_tools:
  - pipeline_classify
  - pipeline_convergence
  - pipeline_save
  - pipeline_load
  - pipeline_clear
  - agent_prompt
  - agent_model
