name: pipeline
version: 3.1.1
description: "Pipeline Plugin v3.1.1 — Kanban-native. Selective context (AGENT_CONTEXT_FIELDS). Best-of-N ensemble (12 tools: +pipeline_ensemble_run, +pipeline_ensemble_judge). Ensemble: config-driven, LLM Judge, kanban subtasks."
author: Hermes + Vladimir
provides_tools:
  - pipeline_classify
  - pipeline_convergence
  - pipeline_save
  - pipeline_load
  - pipeline_clear
  - pipeline_resume
  - pipeline_advance
  - agent_prompt
  - agent_model
  - pipeline_run_agent
  - pipeline_ensemble_run
  - pipeline_ensemble_judge
