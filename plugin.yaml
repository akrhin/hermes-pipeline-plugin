name: pipeline
version: 3.5.0
description: "Pipeline Plugin v3.5.0 — scan_board() now preserves agent order (parsed from parent body instead of ORDER BY), ensemble judge gets real candidate output (not just descriptions), judge config passthrough fixed, pipeline.config.yaml.example added."
author: Hermes + Vladimir
provides_tools:
  - pipeline_classify
  - pipeline_convergence
  - pipeline_save
  - pipeline_load
  - pipeline_clear
  - pipeline_resume
  - pipeline_advance
  - agent_prompt
  - agent_model
  - pipeline_run_agent
  - pipeline_ensemble_run
  - pipeline_ensemble_judge
