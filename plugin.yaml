name: pipeline
version: 3.3.2
description: "Pipeline Plugin v3.3.2 — classify fix: RU keywords (крашит, упал, валит, пофикс), word-boundary fix (crash→crashes), priority order (BUG_KNOWN > BUG_UNKNOWN). 79/79 tests."
author: Hermes + Vladimir
provides_tools:
  - pipeline_classify
  - pipeline_convergence
  - pipeline_save
  - pipeline_load
  - pipeline_clear
  - pipeline_resume
  - pipeline_advance
  - agent_prompt
  - agent_model
  - pipeline_run_agent
  - pipeline_ensemble_run
  - pipeline_ensemble_judge
