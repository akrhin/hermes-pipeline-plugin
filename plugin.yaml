name: pipeline
version: 2.0.0
description: "Pipeline Plugin v2.0 Kanban-native. Variant C: kanban.db is single source of truth, state.json removed. 9 tools: classify, convergence, save, load, clear, resume, advance, prompt, model."
author: Hermes + Vladimir
provides_tools:
  - pipeline_classify
  - pipeline_convergence
  - pipeline_save
  - pipeline_load
  - pipeline_clear
  - pipeline_resume
  - pipeline_advance
  - agent_prompt
  - agent_model
