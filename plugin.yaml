name: pipeline
version: 3.3.1
description: "Pipeline Plugin v3.3.1 — bugfix: Flash prompt (P0), LLM Judge stub (P0), reopen(), maxed_out children, stale cleanup 1-child, scan_board LIMIT 1, integration.prompt dead Full context. 79/79 tests, 0 lint errors."
author: Hermes + Vladimir
provides_tools:
  - pipeline_classify
  - pipeline_convergence
  - pipeline_save
  - pipeline_load
  - pipeline_clear
  - pipeline_resume
  - pipeline_advance
  - agent_prompt
  - agent_model
  - pipeline_run_agent
  - pipeline_ensemble_run
  - pipeline_ensemble_judge
